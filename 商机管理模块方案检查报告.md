# 商机管理模块方案检查报告

## ✅ 已确认事项

1. **客户信息**：扩展现有Client模型，不新建
2. **模块归属**：放在customer_success模块下
3. **立项流程**：赢单后只触发商务经理的待办事项，不直接创建项目或立项申请
4. **报价功能**：项目中心没有报价功能，不复用，新建独立报价功能

---

## 📋 方案检查结果

### ✅ 一、整体设计合理性

**优点：**
- 业务逻辑清晰，覆盖商机全生命周期
- 状态机设计合理（潜在客户→初步接触→需求确认→方案报价→商务谈判→赢单/输单）
- 销售漏斗分析有价值
- 客户分级评定机制完善

**建议：**
- 状态流转需要定义明确的规则和权限控制
- 建议增加状态流转日志记录（类似ContractStatusLog）

---

### ⚠️ 二、需要补充和调整的内容

#### 2.1 Client模型扩展字段

**需要添加的字段：**
```python
# 在现有Client模型中添加：
- unified_credit_code = CharField(统一信用代码)  # 方案中提到
- client_type = CharField(客户类型)  # 开发商/政府单位/设计院/总包单位/其他
- company_scale = CharField(企业规模)  # 大型/中型/小型
- region = CharField(所属区域)
- source = CharField(客户来源)  # 自主开发/老客户推荐/行业展会/网络推广
- score = IntegerField(客户评分, 0-100)  # 用于自动分级
- grade = CharField(客户等级)  # A/B/C级，根据score自动计算
```

**注意：**
- 现有`client_level`字段（vip/important/general/potential）与方案中的A/B/C级客户分级不同
- 建议保留现有字段，新增`grade`字段用于商机管理的分级
- 或者将现有字段扩展，增加A/B/C选项

---

#### 2.2 商机模型设计

**核心模型：BusinessOpportunity**

```python
class BusinessOpportunity(models.Model):
    """商机管理"""
    STATUS_CHOICES = [
        ('potential', '潜在客户'),      # 10%
        ('initial_contact', '初步接触'), # 30%
        ('requirement_confirmed', '需求确认'), # 50%
        ('quotation', '方案报价'),      # 70%
        ('negotiation', '商务谈判'),    # 90%
        ('won', '赢单'),
        ('lost', '输单'),
        ('cancelled', '已取消'),
    ]
    
    # 基本信息
    opportunity_number = CharField(自动生成: OPP-YYYY-NNNN)
    name = CharField(商机名称)
    client = ForeignKey(Client)  # 关联客户
    business_manager = ForeignKey(User)  # 负责商务
    
    # 项目信息
    project_name = CharField(项目名称)
    project_address = CharField(项目地址)
    project_type = CharField(项目业态)  # 住宅/综合体/商业等
    building_area = DecimalField(建筑面积)
    drawing_stage = CharField(图纸阶段)
    
    # 金额和概率
    estimated_amount = DecimalField(预计金额)
    success_probability = IntegerField(成功概率: 10/30/50/70/90)
    weighted_amount = DecimalField(加权金额, 自动计算)
    
    # 状态和时间
    status = CharField(状态)
    urgency = CharField(紧急程度: 普通/紧急/特急)
    expected_sign_date = DateField(预计签约时间)
    actual_sign_date = DateField(实际签约日期, nullable)
    
    # 审批信息
    approval_status = CharField(审批状态: pending/approved/rejected)
    approver = ForeignKey(User, nullable)
    approved_time = DateTimeField(nullable)
    
    # 赢单/输单信息
    actual_amount = DecimalField(实际签约金额, nullable)
    contract_number = CharField(合同编号, nullable)
    win_reason = TextField(赢单原因, nullable)
    loss_reason = TextField(输单原因, nullable)
    
    # 健康度
    health_score = IntegerField(健康度评分, 0-100)
    
    # 审计字段
    created_by = ForeignKey(User)
    created_time = DateTimeField
    updated_time = DateTimeField
```

**需要创建的关联模型：**
1. `OpportunityFollowUp` - 商机跟进记录
2. `OpportunityQuotation` - 报价记录（支持多版本）
3. `OpportunityApproval` - 审批记录
4. `OpportunityStatusLog` - 状态流转日志

---

#### 2.3 待办事项机制

**方案要求：** 赢单后触发商务经理的待办事项

**现有系统：** 有`ProjectTeamNotification`模型，但主要用于项目团队通知

**建议方案：**
1. **方案A（推荐）**：复用`ProjectTeamNotification`，扩展notification_type
   - 添加`business_opportunity_won`类型
   - 在赢单时创建通知给商务经理

2. **方案B**：创建独立的`BusinessTodo`模型
   - 更灵活，但需要额外维护

**推荐使用方案A**，代码示例：
```python
from backend.apps.project_center.models import ProjectTeamNotification

# 赢单时创建待办
ProjectTeamNotification.objects.create(
    project=None,  # 商机阶段还没有项目
    recipient_user=business_manager,
    title=f'商机赢单：{opportunity.name}',
    message=f'商机已赢单，实际签约金额：{actual_amount}万元，请及时处理后续事项。',
    notification_type='business_opportunity_won',
    action_url=f'/business/opportunities/{opportunity.id}/',
    operator=request.user,
)
```

---

#### 2.4 报价管理设计

**模型：OpportunityQuotation**

```python
class OpportunityQuotation(models.Model):
    """商机报价"""
    VERSION_TYPES = [
        ('draft', '初稿报价'),
        ('customer', '客户报价'),
        ('final', '最终报价'),
    ]
    
    opportunity = ForeignKey(BusinessOpportunity)
    version_type = CharField(版本类型)
    version_number = IntegerField(版本号, default=1)
    
    # 报价参数
    building_area = DecimalField(建筑面积)
    project_type = CharField(项目业态)
    service_type = CharField(服务类型)
    service_professions = ManyToManyField(服务专业)
    structure_type = CharField(结构形式)
    
    # 报价结果
    base_quotation = DecimalField(基准报价)
    adjustment_factor = DecimalField(调整系数, default=1.0)
    final_quotation = DecimalField(最终报价)
    quotation_note = TextField(报价说明)
    
    # 文件
    quotation_file = FileField(报价文件, nullable)
    
    created_by = ForeignKey(User)
    created_time = DateTimeField
```

**报价计算逻辑：**
- 需要定义报价规则（费率、单价等）
- 建议创建`QuotationRule`模型存储报价规则
- 或者使用配置表/JSON字段存储规则

---

#### 2.5 客户分级评定

**方案中的评分维度：**
- 企业规模（0-30分）
- 合作历史（0-25分）
- 项目规模（0-20分）
- 付款信誉（0-15分）
- 战略价值（0-10分）

**实现建议：**
1. 在Client模型中添加评分字段
2. 创建评分计算方法（可以放在Client模型的方法中）
3. 根据总分自动计算等级（A/B/C）

**注意：**
- 合作历史需要从`ClientProject`或`BusinessContract`中统计
- 付款信誉需要从`BusinessPaymentPlan`中统计
- 需要定期更新评分（可以设置定时任务）

---

#### 2.6 审批流程

**方案中的审批规则：**
- <20万：商务部经理审批
- 20-100万：商务总监审批
- ≥100万：总经理审批

**实现建议：**
1. 创建`OpportunityApproval`模型（类似`ContractApproval`）
2. 在商机保存时自动判断需要哪些审批层级
3. 创建审批视图和流程

**注意：**
- 需要定义角色代码（如`business_manager`, `business_director`, `general_manager`）
- 审批流程需要支持多级审批

---

#### 2.7 销售漏斗分析

**数据统计需求：**
- 各阶段商机数量和金额
- 阶段转化率
- 加权金额计算
- 月度签约预测

**实现建议：**
1. 使用Django ORM的聚合查询（Count, Sum, Avg）
2. 创建专门的统计视图函数
3. 使用ECharts等图表库展示（前端已有ECharts）

**关键SQL查询：**
```python
# 按状态统计
opportunities = BusinessOpportunity.objects.filter(status__in=['initial_contact', 'requirement_confirmed', ...])
stats = opportunities.values('status').annotate(
    count=Count('id'),
    total_amount=Sum('estimated_amount'),
    weighted_amount=Sum(F('estimated_amount') * F('success_probability') / 100)
)
```

---

#### 2.8 权限设计

**需要添加的权限：**
```python
PERMISSION_ITEMS = [
    # 商机管理基础权限
    {"code": "customer_success.opportunity.view", "name": "商机管理-查看"},
    {"code": "customer_success.opportunity.create", "name": "商机管理-创建"},
    {"code": "customer_success.opportunity.manage", "name": "商机管理-管理"},
    {"code": "customer_success.opportunity.approve", "name": "商机管理-审批"},
    
    # 报价管理权限
    {"code": "customer_success.quotation.view", "name": "报价管理-查看"},
    {"code": "customer_success.quotation.create", "name": "报价管理-创建"},
    {"code": "customer_success.quotation.manage", "name": "报价管理-管理"},
]
```

**角色权限映射：**
- 商务经理：查看分配商机、创建和更新商机、记录跟进
- 商务部经理/商务总监：查看全部商机、审批报价
- 总经理：全量数据访问

---

#### 2.9 菜单集成

**需要在home页"商务中心"菜单下添加：**
```python
{'label': '商机管理', 'url_name': 'business_pages:opportunity_management', 'permission': 'customer_success.opportunity.view'},
{'label': '报价管理', 'url_name': 'business_pages:quotation_management', 'permission': 'customer_success.quotation.view'},
```

---

### ⚠️ 三、潜在问题和风险

#### 3.1 数据一致性
- **风险**：商机状态与客户信息不一致
- **应对**：使用事务确保数据一致性，添加数据校验

#### 3.2 性能问题
- **风险**：大量商机数据导致统计查询慢
- **应对**：添加数据库索引，使用缓存，分页加载

#### 3.3 权限控制
- **风险**：未授权用户访问敏感商机信息
- **应对**：严格权限控制，所有操作都需要权限检查

#### 3.4 报价规则维护
- **风险**：报价规则变更频繁，需要灵活配置
- **应对**：使用配置表或JSON字段存储规则，支持版本管理

---

### ✅ 四、技术实现建议

#### 4.1 数据模型
- 扩展现有Client模型（添加字段）
- 创建BusinessOpportunity及相关模型
- 创建OpportunityQuotation模型
- 创建OpportunityFollowUp模型

#### 4.2 视图和路由
- 在`customer_success/views_pages.py`中添加商机管理视图
- 在`customer_success/urls.py`中添加路由
- 创建商机列表、详情、创建、编辑、审批等页面

#### 4.3 前端页面
- 使用现有模板风格（参考contract_management）
- 销售漏斗使用ECharts图表
- 响应式设计，支持移动端

#### 4.4 集成点
- 赢单时创建ProjectTeamNotification通知商务经理
- 客户信息与现有Client模型共享
- 报价功能独立实现，不与项目中心复用

---

## 📝 五、开发优先级建议

### 第一阶段：核心功能（必须）
1. ✅ 扩展Client模型字段
2. ✅ 创建BusinessOpportunity模型
3. ✅ 创建商机列表、详情、创建、编辑页面
4. ✅ 实现状态流转逻辑

### 第二阶段：跟进和报价（重要）
5. ✅ 创建OpportunityFollowUp模型和跟进记录功能
6. ✅ 创建OpportunityQuotation模型和报价功能
7. ✅ 实现报价计算逻辑

### 第三阶段：审批和分析（增强）
8. ✅ 实现审批流程
9. ✅ 实现销售漏斗分析
10. ✅ 实现客户分级评定

### 第四阶段：优化和扩展（可选）
11. ✅ 移动端优化
12. ✅ 数据导出功能
13. ✅ 高级统计分析

---

## ✅ 六、总结

**方案整体评价：** ⭐⭐⭐⭐ (4/5)

**优点：**
- 业务逻辑完整，覆盖商机全生命周期
- 设计思路清晰，符合实际业务需求
- 与现有系统集成点明确

**需要补充：**
- 客户模型扩展字段定义
- 报价规则配置方案
- 待办事项具体实现方式
- 数据统计的具体指标定义

**建议：**
- 可以先实现核心功能（商机CRUD、状态流转）
- 再逐步完善跟进、报价、审批等功能
- 最后实现分析和报表功能

---

## 🚀 确认后可以开始开发

请确认以上检查结果，如有需要调整的地方请告知，确认后即可开始开发工作。

