# 发文审批流程节点说明

**更新时间**: 2025-01-27

---

## 📋 流程概述

**流程名称**: 发文审批流程  
**流程代码**: `outgoing_document_approval`  
**流程描述**: 经办人（创建人）提交后，需要经过多级上级审批，最后总经理审批通过后才能发出

**流程结构**: 经办人 → 多级上级审批 → 总经理审批 → 结束

---

## 🔄 流程节点详情

### 节点 0: 开始节点

- **节点名称**: 开始
- **节点类型**: 开始节点 (`start`)
- **节点顺序**: 0
- **审批人类型**: 无
- **描述**: 审批流程开始节点，经办人（创建人）提交发文申请

**说明**:
- 这是流程的起始节点
- 当经办人创建发文并提交审批时，流程从此节点开始
- 不需要审批操作，自动进入下一个节点

---

### 节点 1: 多级上级审批

- **节点名称**: 多级上级审批
- **节点类型**: 审批节点 (`approval`)
- **节点顺序**: 1
- **审批人类型**: 创建人上级 (`creator_manager`)
- **审批模式**: 单人审批 (`single`)
- **是否必审**: 是
- **可驳回**: 是
- **可转交**: 否
- **超时时间**: 24小时
- **描述**: 创建人的直接上级审批，如果上级还有上级，会逐级向上审批。审批通过后进入下一节点。

**审批人识别逻辑**:
1. **方法1**: 查找创建人部门的部门负责人（`Department.leader`）
2. **方法2**: 查找创建人部门中具有部门经理角色的用户（`Role.code='department_manager'`）
3. **方法3**: 按优先级查找上级角色：
   - 总经理 (`general_manager`)
   - 商务总监 (`business_director`)
   - 部门经理 (`department_manager`)
   - 商务部经理 (`business_manager`)
   - 技术部经理 (`technical_manager`)
   - 行政主管 (`admin_office`)
4. **方法4**: 如果同部门没找到，查找其他部门中具有总经理角色的用户

**多级审批机制**:
- 系统会自动找到创建人的直接上级进行审批
- 如果上级审批通过，会继续向上级审批（如果有）
- 直到没有上级或到达指定层级，进入下一节点
- 每次审批只找一级上级，逐级向上

---

### 节点 2: 总经理审批

- **节点名称**: 总经理审批
- **节点类型**: 审批节点 (`approval`)
- **节点顺序**: 2
- **审批人类型**: 指定角色 (`role`)
- **审批角色**: 总经理 (`general_manager`)
- **审批模式**: 单人审批 (`single`)
- **是否必审**: 是
- **可驳回**: 是
- **可转交**: 否
- **超时时间**: 24小时
- **描述**: 总经理最终审批发文申请，审批通过后可以发出文件

**审批人识别**:
- 系统查找所有具有 `general_manager` 角色的用户
- 如果申请人有部门，优先找同部门的总经理；否则找第一个总经理

---

### 节点 3: 结束节点

- **节点名称**: 结束
- **节点类型**: 结束节点 (`end`)
- **节点顺序**: 3
- **审批人类型**: 无
- **描述**: 审批流程结束节点，审批通过后可以发出文件

**说明**:
- 这是流程的结束节点
- 当总经理审批通过后，流程进入此节点
- 流程状态变为 `approved`（已通过）
- 发文状态同步更新为 `approved`（已审批）

---

## 🔄 流程执行流程

```
┌─────────┐
│  开始   │ 经办人提交发文申请
└────┬────┘
     │
     ▼
┌─────────────────┐
│ 多级上级审批    │ 创建人的直接上级审批
│ (逐级向上)      │ → 如果还有上级，继续向上审批
└────┬────────────┘
     │
     ▼
┌─────────────┐
│ 总经理审批  │ 总经理角色用户审批
└────┬────────┘
     │
     ▼
┌─────────┐
│  结束   │ 审批完成，可以发出文件
└─────────┘
```

---

## ⚙️ 流程配置

### 流程设置

- **允许撤回**: ✅ 是
- **允许驳回**: ✅ 是
- **允许转交**: ❌ 否
- **超时时间**: 24小时（每个节点）
- **超时处理**: 仅通知 (`notify`)

### 节点配置

| 节点 | 名称 | 类型 | 审批人 | 审批模式 | 超时 | 可驳回 | 可转交 |
|------|------|------|--------|----------|------|--------|--------|
| 0 | 开始 | 开始节点 | - | - | - | - | - |
| 1 | 多级上级审批 | 审批节点 | 创建人上级 | 单人审批 | 24小时 | ✅ | ❌ |
| 2 | 总经理审批 | 审批节点 | 总经理角色 | 单人审批 | 24小时 | ✅ | ❌ |
| 3 | 结束 | 结束节点 | - | - | - | - | - |

---

## 📝 使用说明

### 1. 启动审批流程

在发文提交审批时，启动审批流程：

```python
from backend.apps.workflow_engine.models import WorkflowTemplate
from backend.apps.workflow_engine.services import ApprovalEngine

# 获取流程模板
workflow = WorkflowTemplate.objects.get(code='outgoing_document_approval')

# 启动审批流程
instance = ApprovalEngine.start_approval(
    workflow=workflow,
    content_object=outgoing_document,  # OutgoingDocument实例
    applicant=request.user,            # 经办人（创建人）
    comment='提交发文审批'              # 申请说明
)

# 更新发文状态
outgoing_document.status = 'reviewing'
outgoing_document.save()
```

### 2. 审批流程

1. **经办人提交**: 创建发文并提交审批，流程从"开始"节点进入"多级上级审批"节点
2. **多级上级审批**: 
   - 系统自动找到创建人的直接上级
   - 上级审批通过后，如果还有上级，继续向上审批
   - 直到没有上级或到达指定层级，进入下一节点
3. **总经理审批**: 总经理角色用户进行最终审批
4. **审批完成**: 审批通过后，流程结束，发文可以发出

### 3. 审批操作

- **通过**: 审批通过，进入下一节点或完成流程
- **驳回**: 审批驳回，流程终止，需要重新提交
- **撤回**: 如果流程配置允许，申请人可以撤回申请

---

## ⚠️ 注意事项

1. **上级关系配置**: 
   - 确保用户设置了正确的上级关系（在用户管理中配置）
   - 系统会按优先级查找上级：部门负责人 → 部门经理角色 → 其他高级角色

2. **总经理角色**: 
   - 确保系统中存在 `general_manager` 角色
   - 确保有用户被分配了该角色

3. **超时处理**: 
   - 每个节点审批超时时间为24小时
   - 超时后仅发送通知，不会自动处理

4. **多级审批**: 
   - 多级上级审批会自动识别创建人的上级关系
   - 每次审批只找一级上级，逐级向上
   - 如果上级审批通过，会继续向上级审批（如果有）

---

## 🔍 查看流程配置

### 在系统中查看

1. 访问工作流引擎首页: `/workflow/home/`
2. 进入流程列表: `/workflow/workflows/`
3. 找到"发文审批流程"
4. 点击"查看详情"查看所有节点配置

### 通过管理命令查看

```bash
python manage.py setup_outgoing_document_approval_workflow
```

---

**文档更新时间**: 2025-01-27

