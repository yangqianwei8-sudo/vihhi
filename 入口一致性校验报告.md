# 入口一致性校验报告（只读）

**生成时间**: 2025-01-XX  
**校验类型**: 只读勘察（未修改任何代码）  
**目的**: 锁定当前系统的入口配置，为 plan_management 的"干净重来"奠定基础

---

## 一、核心入口配置（事实层）

### 1.1 代码目录
**实际运行目录**:
```
/home/devbox/project/vihhi/weihai_tech_production_system
```

**证据**: 
- `start_services.sh:4`: `PROJECT_DIR="/home/devbox/project/vihhi/weihai_tech_production_system"`
- `pwd` 命令输出: `/home/devbox/project/vihhi/weihai_tech_production_system`

### 1.2 Settings 模块
**实际运行的 Settings**:
```
backend.config.settings
```

**证据**:
- `manage.py:6`: `os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'backend.config.settings')`
- `backend/config/wsgi.py:39`: `os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'backend.config.settings')`
- Django shell 输出: `SETTINGS_MODULE: backend.config.settings`

**Settings 文件路径**:
```
/home/devbox/project/vihhi/weihai_tech_production_system/backend/config/settings.py
```

**BASE_DIR 值**:
```
/home/devbox/project/vihhi/weihai_tech_production_system/backend
```

**证据**: Django shell 输出: `BASE_DIR: /home/devbox/project/vihhi/weihai_tech_production_system/backend`

### 1.3 WSGI 入口
**实际运行的 WSGI**:
```
backend.config.wsgi:application
```

**证据**:
- `start_services.sh:81`: `backend.config.wsgi:application`
- `ps aux | grep gunicorn` 输出: `backend.config.wsgi:application`

**WSGI 文件路径**:
```
/home/devbox/project/vihhi/weihai_tech_production_system/backend/config/wsgi.py
```

### 1.4 URL 配置
**ROOT_URLCONF**:
```
backend.config.urls
```

**证据**:
- `backend/config/settings.py:83`: `ROOT_URLCONF = 'backend.config.urls'`
- Django shell 输出: `ROOT_URLCONF: backend.config.urls`

**URL 文件路径**:
```
/home/devbox/project/vihhi/weihai_tech_production_system/backend/config/urls.py
```

### 1.5 Gunicorn 启动脚本
**启动脚本路径**:
```
/home/devbox/project/vihhi/weihai_tech_production_system/start_services.sh
```

**Gunicorn 启动命令**（从 `start_services.sh:72-81`）:
```bash
gunicorn \
    --bind 0.0.0.0:8001 \
    --workers 8 \
    --worker-class sync \
    --timeout 120 \
    --max-requests 1000 \
    --max-requests-jitter 50 \
    --access-logfile /tmp/gunicorn_access.log \
    --error-logfile /tmp/gunicorn_error.log \
    backend.config.wsgi:application
```

**运行状态**:
- 端口: `8001`
- Workers: `8`
- 进程数: `9` (1 master + 8 workers)
- 状态: ✅ 运行中

**证据**: `ps aux | grep gunicorn` 输出显示 9 个进程

### 1.6 数据库配置
**数据库连接方式**:
- 优先使用环境变量 `DATABASE_URL`
- 开发环境默认: `postgresql://postgres:zdg7xx28@dbconn.sealosbja.site:38013/postgres`

**证据**:
- `backend/config/settings.py:110-112`: `DEVELOPMENT_DATABASE_URL`
- `backend/config/settings.py:116`: `database_url = os.getenv('DATABASE_URL', '').strip()`

---

## 二、模块清单（16个模块）

### 2.1 INSTALLED_APPS 清单
从 `backend/config/settings.py:27-66` 统计:

1. `django.contrib.admin`
2. `django.contrib.auth`
3. `django.contrib.contenttypes`
4. `django.contrib.sessions`
5. `django.contrib.messages`
6. `django.contrib.staticfiles`
7. `django.contrib.postgres`
8. `rest_framework`
9. `corsheaders`
10. `django_filters`
11. `backend.apps.permission_management.apps.PermissionManagementConfig`
12. `backend.apps.system_management.apps.SystemManagementConfig`
13. `backend.apps.production_management.apps.ProductionManagementConfig`
14. `backend.apps.customer_management.apps.CustomerManagementConfig`
15. `backend.apps.resource_standard`
16. `backend.apps.settlement_management.apps.SettlementManagementConfig`
17. `backend.apps.settlement_center.apps.SettlementCenterConfig`
18. `backend.apps.risk_management`
19. `backend.apps.administrative_management.apps.AdministrativeManagementConfig`
20. `backend.apps.financial_management.apps.FinancialManagementConfig`
21. `backend.apps.personnel_management.apps.PersonnelManagementConfig`
22. `backend.apps.workflow_engine.apps.WorkflowEngineConfig`
23. `backend.apps.delivery_customer.apps.DeliveryCustomerConfig`
24. `backend.apps.archive_management.apps.ArchiveManagementConfig`
25. `backend.apps.litigation_management.apps.LitigationManagementConfig`
26. `backend.apps.plan_management.apps.PlanManagementConfig` ⭐ **目标模块**
27. `backend.apps.api_management.apps.ApiManagementConfig`

**总计**: 27 个应用（包含 Django 内置应用）

### 2.2 模块目录清单
从 `backend/apps/` 目录统计（排除 `__pycache__`、`migrations` 等）:

1. `permission_management` ✅
2. `system_management` ✅
3. `production_management` ✅
4. `customer_management` ✅
5. `resource_standard` ✅
6. `settlement_management` ✅
7. `settlement_center` ✅
8. `risk_management` ✅
9. `administrative_management` ✅
10. `financial_management` ✅
11. `personnel_management` ✅
12. `workflow_engine` ✅
13. `delivery_customer` ✅
14. `archive_management` ✅
15. `litigation_management` ✅
16. `plan_management` ⭐ **目标模块**
17. `api_management` ✅

**总计**: 17 个业务模块

---

## 三、Plan Management 模块入口现状

### 3.1 URL 路由配置
**根 URL 配置** (`backend/config/urls.py:91`):
```python
path('plan/', include(('backend.apps.plan_management.urls_pages', 'plan_pages'), namespace='plan_pages')),
```

**Plan Management URLs 文件**:
- API 路由: `backend/apps/plan_management/urls.py`
- 页面路由: `backend/apps/plan_management/urls_pages.py` ⭐ **实际使用**

### 3.2 模块文件结构
从 `find backend/apps/plan_management -name "*.py"` 输出:

- `__init__.py`
- `apps.py`
- `admin.py`
- `urls.py` (API 路由)
- `urls_pages.py` (页面路由) ⭐
- `views_pages.py`
- `migrations/` (14 个迁移文件)

---

## 四、潜在风险点（只读诊断）

### 4.1 迁移依赖错误 ⚠️
**错误信息**:
```
django.db.migrations.exceptions.NodeNotFoundError: 
Migration production_management.0044_businesscontract_company_and_more 
dependencies reference nonexistent parent node ('org', '0001_initial')
```

**影响范围**: 
- `production_management` 模块
- **不影响 plan_management**（独立模块）

**诊断**:
- 这是 `production_management` 模块的迁移依赖问题
- `plan_management` 可以独立工作，不受此影响

**证据**: `python manage.py showmigrations --plan` 命令输出

### 4.2 模块导入阻断检查（待执行）
**建议检查项**:
1. 各模块的 `apps.py` 是否可以正常导入
2. 各模块的 `admin.py` 是否可以正常导入
3. 各模块的 `urls.py` 是否可以正常导入（不会在启动时阻断）

**方法**:
```python
# 只读检查，不修改代码
python manage.py check --deploy
```

---

## 五、入口一致性总结

### ✅ 一致性确认
1. **代码目录**: `/home/devbox/project/vihhi/weihai_tech_production_system` ✅
2. **Settings 模块**: `backend.config.settings` ✅
3. **WSGI 入口**: `backend.config.wsgi:application` ✅
4. **URL 配置**: `backend.config.urls` ✅
5. **Gunicorn 启动**: 使用 `backend.config.wsgi:application` ✅
6. **BASE_DIR**: `/home/devbox/project/vihhi/weihai_tech_production_system/backend` ✅

### 📋 下一步建议
1. **执行模块导入阻断检查**（只读，不改代码）
2. **锁定此入口配置**（作为"干净重来"的基准）
3. **开始 plan_management 的闭环重建**（只改 plan_management，不改其他模块）

---

## 六、三条铁律验证

### 铁律 1: 入口一致 ✅
- **Gunicorn 启动脚本**: `start_services.sh` → `backend.config.wsgi:application`
- **Settings**: `backend.config.settings`
- **代码目录**: `/home/devbox/project/vihhi/weihai_tech_production_system`
- **数据库**: 环境变量 `DATABASE_URL` 或开发默认值
- **✅ 一致性确认**: 所有入口点指向同一配置

### 铁律 2: 最小闭环提交
- **状态**: 待执行（开始 plan_management 重构后）
- **建议**: 每完成一个可验证的小目标就 commit

### 铁律 3: 绝不用占位函数
- **状态**: 待执行（开始 plan_management 重构后）
- **承诺**: 不做临时占位，不做"能启动就行"的妥协

---

**报告结束**
