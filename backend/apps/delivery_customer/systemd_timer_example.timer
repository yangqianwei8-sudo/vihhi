[Unit]
Description=发文跟踪状态更新定时器
Requires=delivery-tracking-update.service

[Timer]
# 每30分钟执行一次
OnCalendar=*:0/30
# 或者使用其他格式：
# OnCalendar=daily  # 每天执行
# OnCalendar=hourly  # 每小时执行
# OnCalendar=*-*-* 09:00:00  # 每天上午9点

# 如果系统启动时错过了执行时间，立即执行一次
Persistent=true

[Install]
WantedBy=timers.target
