# P0 执行总结（多公司隔离整改）

## 执行时间
2024年（按P0→P1顺序执行）

---

## ✅ 已完成任务

### P0-1: 工作流审批选人禁止跨公司（已完成）

**改动文件**:
- `apps/workflow_engine/services.py`
  - 新增 `CompanyScopeNotResolved` 异常类
  - 修改 `_get_approvers()` 方法，添加公司隔离安全阀

**效果**: 审批选人已禁止跨公司，无法确定 company 时直接拒绝流转。

---

### P0-2: 给User增加company外键（已完成）

**改动文件**:
1. `apps/system_management/models.py` - User 模型新增 `company` 字段
2. `apps/plan_management/utils.py` - `apply_company_scope()` 改为使用 `user.company_id`
3. `apps/plan_management/views.py` - `_get_profile_org()` 改为使用 `user.company`
4. `apps/plan_management/notifications.py` - `profile__company` 改为 `company`
5. `apps/plan_management/services/todo_generator.py` - `profile__company` 改为 `company`
6. `apps/plan_management/views_stats.py` - `profile.company_id` 改为 `user.company_id`
7. `apps/plan_management/management/commands/create_test_plans.py` - `profile.company` 改为 `user.company`
8. `apps/system_management/admin.py` - UserAdmin 添加 `company` 字段显示和过滤

**已创建文件**:
1. `apps/system_management/migrations/0014_add_user_company_field.py` - Migration 文件
2. `apps/system_management/management/commands/backfill_user_company.py` - 数据回填脚本（已完善）
3. `apps/system_management/management/commands/dump_companies.py` - 公司信息查看命令
4. `apps/system_management/management/commands/set_default_company.py` - 默认公司设置命令
5. `apps/system_management/management/commands/backfill_department_company.py` - 部门回填脚本（P0-3准备）
6. `apps/system_management/management/commands/backfill_role_company.py` - 角色回填脚本（P0-4准备）
7. `apps/plan_management/management/commands/backfill_plan_company.py` - 计划回填脚本（P0-5准备）
8. `apps/config/middleware.py` - 新增 `CompanyIsolationGuardMiddleware` 类
9. `templates/shared/company_not_configured.html` - 错误提示页面
10. `docs/公司ID对照表.md` - 公司ID对照表文档

**已修改配置**:
- `config/settings.py` - MIDDLEWARE 中添加 `CompanyIsolationGuardMiddleware`

---

## ⏳ 待执行任务

### P0-3: Department增加company + 唯一约束改造（待执行）

**需要改动**:
1. `apps/system_management/models.py` - Department 模型新增 `company` 字段
2. 移除 `code` 字段的 `unique=True`
3. 添加 `UniqueConstraint(fields=['company', 'code'], name='uniq_dept_company_code')`
4. 创建 migration（分两步：先添加字段，再添加约束）
5. 运行回填脚本: `python manage.py backfill_department_company --default-company=1`

**验收用例**:
- 同一个 `code="FIN"` 可以在 A/B 两家公司各建一个部门，不冲突
- 组织树接口在 A 公司用户下，不返回 B 公司部门

---

### P0-4: Role增加company + 唯一约束改造（待执行）

**需要改动**:
1. `apps/system_management/models.py` - Role 模型新增 `company` 字段（允许 NULL）
2. 移除 `code` 字段的 `unique=True`
3. 添加 `UniqueConstraint(fields=['company', 'code'], name='uniq_role_company_code')`
4. 创建 migration
5. 运行回填脚本: `python manage.py backfill_role_company` (保持现有角色为全局)

**验收用例**:
- A/B 公司都能建 `code="finance_director"` 的公司角色
- company=NULL 的全局角色仍然可建且可分配

---

### P0-5: Plan.company强制写入 + 默认过滤（待执行）

**需要改动**:
1. `apps/plan_management/models.py` - Plan 模型的 `company` 字段改为必填（分两步迁移）
2. 运行回填脚本: `python manage.py backfill_plan_company --default-company=1`
3. 所有 queryset 入口统一走 `apply_company_scope(qs, user)`
4. 创建/保存时：`plan.company = request.user.company`（如果为空则拒绝创建）

**验收用例**:
- A 公司用户只能看到 A 公司的 Plan
- 创建 Plan 时 company 自动写入且不允许为空

---

## 📋 立即执行步骤（P0-2 收尾）

### 1. 查看公司列表
```bash
cd /home/devbox/project/vihhi/weihai_tech_production_system
python manage.py dump_companies
```

### 2. 确定默认公司ID（通常是集团公司）
```bash
# 查看推荐默认公司
python manage.py set_default_company --show
```

### 3. 执行 migration
```bash
python manage.py migrate system_management
```

### 4. 回填用户公司（使用集团公司兜底）
```bash
# 假设集团公司ID=1，根据实际dump_companies输出调整
python manage.py backfill_user_company --default-company=1
```

### 5. 验证
```bash
# Django shell
python manage.py shell
>>> from backend.apps.system_management.models import User
>>> User.objects.filter(company__isnull=True).count()  # 应该为0
>>> User.objects.first().company_id  # 应该有值
```

### 6. 测试安全门槛
- 创建一个 company 为空的测试用户
- 尝试访问业务模块（如 `/plan/plans/`）
- 应该被 middleware 拦截并显示友好提示

---

## 📊 变更文件清单

### 已修改文件（11个）
1. `apps/workflow_engine/services.py` - P0-1
2. `apps/system_management/models.py` - P0-2
3. `apps/plan_management/utils.py` - P0-2
4. `apps/plan_management/views.py` - P0-2
5. `apps/plan_management/notifications.py` - P0-2
6. `apps/plan_management/services/todo_generator.py` - P0-2
7. `apps/plan_management/views_stats.py` - P0-2
8. `apps/plan_management/management/commands/create_test_plans.py` - P0-2
9. `apps/system_management/admin.py` - P0-2
10. `apps/config/middleware.py` - P0-2补充
11. `config/settings.py` - P0-2补充

### 已创建文件（10个）
1. `apps/system_management/migrations/0014_add_user_company_field.py`
2. `apps/system_management/management/commands/backfill_user_company.py`
3. `apps/system_management/management/commands/dump_companies.py`
4. `apps/system_management/management/commands/set_default_company.py`
5. `apps/system_management/management/commands/backfill_department_company.py`
6. `apps/system_management/management/commands/backfill_role_company.py`
7. `apps/plan_management/management/commands/backfill_plan_company.py`
8. `templates/shared/company_not_configured.html`
9. `docs/公司ID对照表.md`
10. `docs/多公司隔离整改进度.md`

---

## ⚠️ 重要提示

### 执行 migration 前
1. **备份数据库**
2. **确认默认公司ID**（运行 `dump_companies` 查看）
3. **准备回填策略**（使用集团公司兜底）

### 执行 migration 后
1. **立即运行回填脚本**（避免系统不可用）
2. **验证安全门槛**（company 为空的用户应被拦截）
3. **在 Admin 中逐个调整**（将各子公司员工调整到各自公司）

### 安全门槛说明
- **已启用**: `CompanyIsolationGuardMiddleware` 已在 settings.py 中注册
- **效果**: company 为空的用户访问业务模块时，会被拦截并显示友好提示
- **超管豁免**: 超管不受此限制（可查看所有公司数据）

---

## 📝 下一步

1. **执行 P0-2 收尾步骤**（migration + 回填）
2. **继续执行 P0-3**（Department.company）
3. **继续执行 P0-4**（Role.company）
4. **继续执行 P0-5**（Plan.company 强制写入）
5. **进入 P1**（逐模块补 company 字段）

---

## 验收检查清单

### P0-1 验收
- [ ] 构造两家公司 A/B，各有同名角色/部门
- [ ] 发起 A 公司审批：审批人不得出现 B 公司用户
- [ ] 如果 instance.company_id 为空：必须阻止发起/流转

### P0-2 验收
- [ ] Django shell: `User.objects.filter(company__isnull=True).count()` 为 0
- [ ] 任意用户登录后，后端能读到 `request.user.company_id`
- [ ] company 为空的用户访问业务模块时，被 middleware 拦截

### P0-3 验收（待执行）
- [ ] 同一个 `code="FIN"` 可以在 A/B 两家公司各建一个部门，不冲突
- [ ] 组织树接口在 A 公司用户下，不返回 B 公司部门

### P0-4 验收（待执行）
- [ ] A/B 公司都能建 `code="finance_director"` 的公司角色
- [ ] company=NULL 的全局角色仍然可建且可分配

### P0-5 验收（待执行）
- [ ] A 公司用户只能看到 A 公司的 Plan
- [ ] 创建 Plan 时 company 自动写入且不允许为空
