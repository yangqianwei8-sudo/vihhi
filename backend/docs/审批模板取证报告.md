# 📋 审批模板取证报告

**取证时间**: 2026-02-05  
**取证原则**: 只读、不修改任何业务逻辑  
**取证目的**: 建立证据链，为后续治理提供事实基础

---

## 一、审批模板全量清单

### 1.1 数据库中的审批模板（共10个）

| 模板ID | 模板代码 | 模板名称 | 状态 | 分类 | 适用模型 | 审批实例数 |
|--------|---------|---------|------|------|---------|-----------|
| 36 | `administrative_general_approval` | 行政通用审批流程 | active | 行政管理 | `[]` | 0 |
| 38 | `goal_adjustment_approval` | 目标调整申请审批流程 | active | 计划管理 | `['strategicgoal']` | 0 |
| 37 | `goal_creation_approval` | 创建目标审批流程 | active | 计划管理 | `['strategicgoal']` | 0 |
| 34 | `loan_approval` | 借款申请审批流程 | active | 行政管理 | `['loanapplication']` | 6 |
| 35 | `opportunity_approval` | 商机审批流程 | active | 商机管理 | `['businessopportunity']` | 26 |
| 33 | `plan_cancel_approval` | 计划取消审批 | active | 计划管理 | `['plan']` | 3 |
| 32 | `plan_start_approval` | 计划启动审批 | active | 计划管理 | `['plan']` | 73 |
| 30 | `seal_borrowing_approval` | 印章借用审批流程 | active | 行政管理 | `['{sealborrowing}', 'sealborrowing']` | 1 |
| 31 | `seal_usage_approval` | 用印申请审批流程 | active | 行政管理 | `['{sealusage}']` | 12 |

### 1.2 模板定义来源

**模型定义位置**:
- 文件: `backend/apps/workflow_engine/models.py`
- 模型类: `WorkflowTemplate`
- 数据库表: `workflow_template`

**Admin注册位置**:
- 文件: `backend/apps/workflow_engine/admin.py`
- 类: `WorkflowTemplateAdmin`

**初始化脚本位置**:
- `backend/apps/workflow_engine/management/commands/setup_loan_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_seal_borrowing_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_seal_usage_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_opportunity_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_goal_creation_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_goal_adjustment_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_customer_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_contact_approval_workflow.py`
- `backend/apps/workflow_engine/management/commands/setup_relationship_upgrade_approval_workflow.py`
- `backend/apps/plan_management/management/commands/init_plan_approval_workflows.py`
- `backend/apps/plan_management/management/commands/setup_plan_approval_workflow_v2.py`

---

## 二、模板 ↔ 业务对象绑定关系

### 2.1 绑定方式

**通用绑定机制**:
- 使用 Django ContentType 框架
- `ApprovalInstance.content_type` (ForeignKey to ContentType)
- `ApprovalInstance.object_id` (PositiveIntegerField)
- 通过 `applicable_models` 字段（ArrayField）配置模板适用的模型

### 2.2 模板与业务对象映射表

| 模板代码 | 业务模型 | 绑定方式 | 是否被多个业务对象复用 |
|---------|---------|---------|---------------------|
| `loan_approval` | `LoanApplication` | `applicable_models: ['loanapplication']` | 否 |
| `seal_borrowing_approval` | `SealBorrowing` | `applicable_models: ['sealborrowing']` | 否 |
| `seal_usage_approval` | `SealUsage` | `applicable_models: ['sealusage']` | 否 |
| `opportunity_approval` | `BusinessOpportunity` | `applicable_models: ['businessopportunity']` | 否 |
| `plan_start_approval` | `Plan` | `applicable_models: ['plan']` | 否 |
| `plan_cancel_approval` | `Plan` | `applicable_models: ['plan']` | 否（同一模型，不同流程） |
| `goal_creation_approval` | `StrategicGoal` | `applicable_models: ['strategicgoal']` | 否 |
| `goal_adjustment_approval` | `StrategicGoal` | `applicable_models: ['strategicgoal']` | 否（同一模型，不同流程） |
| `administrative_general_approval` | 未配置 | `applicable_models: []` | 是（通用流程，但未使用） |

### 2.3 实际审批实例统计（按业务对象类型）

| 业务对象类型 | 审批实例数 | 使用的模板 |
|------------|-----------|-----------|
| `plan` | 76 | `plan_start_approval` (73), `plan_cancel_approval` (3) |
| `businessopportunity` | 26 | `opportunity_approval` |
| `sealusage` | 12 | `seal_usage_approval` |
| `loanapplication` | 6 | `loan_approval` |
| `sealborrowing` | 1 | `seal_borrowing_approval` |

---

## 三、审批触发入口清单

### 3.1 直接调用 ApprovalEngine.start_approval

**位置1**: `backend/apps/contract_management/views_pages.py`
- 函数: `contract_submit_approval` (行1334)
- 触发条件: POST请求，合同状态为 `draft` 或 `pending_review`
- 流程代码: `contract_approval` 或 `customer_management_approval`（回退）
- 关键参数: `workflow`, `content_object=contract`, `applicant=request.user`

**位置2**: `backend/apps/opportunity_management/views_opportunity.py`
- 函数: `opportunity_submit_for_approval` (行548)
- 触发条件: POST请求，商机无待审批实例
- 流程代码: `opportunity_approval`
- 关键参数: `workflow`, `content_object=opportunity`, `applicant=request.user`

### 3.2 通过 UniversalApprovalService 提交

**位置3**: `backend/apps/administrative_management/views_pages.py`
- 函数: `loan_create` (行7757)
- 服务类: `LoanApprovalService`
- 触发条件: 创建借款申请后自动提交
- 流程代码: `loan_approval` (通过 `WORKFLOW_CODE` 常量)

**位置4**: `backend/apps/administrative_management/views_pages.py`
- 函数: `seal_borrowing_create` (行2661)
- 服务类: `SealBorrowingApprovalService`
- 触发条件: 创建印章借用后自动提交
- 流程代码: `seal_borrowing_approval`

**位置5**: `backend/apps/administrative_management/views_pages.py`
- 函数: `seal_usage_create` (行2885)
- 服务类: `SealUsageApprovalService`
- 触发条件: 创建用印申请后自动提交
- 流程代码: `seal_usage_approval`

### 3.3 通过 PlanApprovalService 提交

**位置6**: `backend/apps/plan_management/services/plan_approval.py`
- 函数: `PlanApprovalService.submit_start_approval` (行36)
- 触发条件: 计划状态为 `draft`
- 流程代码: `plan_start_approval`
- 调用位置:
  - `backend/apps/plan_management/views.py` (行1178, `submit_approval` action)
  - `backend/apps/plan_management/services/plan_decisions.py` (行48, `request_start` 函数)

**位置7**: `backend/apps/plan_management/services/plan_approval.py`
- 函数: `PlanApprovalService.submit_cancel_approval` (行92)
- 触发条件: 计划状态为 `in_progress`
- 流程代码: `plan_cancel_approval`
- 调用位置:
  - `backend/apps/plan_management/services/plan_decisions.py` (行103, `request_cancel` 函数)

### 3.4 触发方式总结

| 触发方式 | 数量 | 说明 |
|---------|------|------|
| 手动提交（View函数） | 2 | 合同审批、商机审批 |
| 自动触发（创建后） | 3 | 借款申请、印章借用、用印申请 |
| 服务层调用 | 2 | 计划启动、计划取消 |
| **总计** | **7** | |

---

## 四、审批通过/驳回后的业务行为代码清单

### 4.1 审批引擎中的业务逻辑（核心位置）

**文件**: `backend/apps/workflow_engine/services.py`

**函数**: `ApprovalEngine._update_business_object_status` (行705)

**审批通过后的行为** (行733-762):

1. **商机** (`businessopportunity`):
   - 位置: 行718-729
   - 行为: 更新 `approval_status='approved'`, `approver`, `approved_time`

2. **印章借用** (`sealborrowing`):
   - 位置: 行744-750
   - 行为: 
     - 更新 `status='approved'`
     - 更新关联印章状态 `seal.status='borrowed'`

3. **用印申请** (`sealusage`):
   - 位置: 行751-754
   - 行为: 仅记录日志，不更新状态

4. **其他业务对象**:
   - 位置: 行756-759
   - 行为: 检查 `STATUS_CHOICES`，如果存在 `approved` 则设置 `status='approved'`

**审批驳回后的行为** (行764-780):

1. **印章借用** (`sealborrowing`):
   - 位置: 行766-767
   - 行为: 更新 `status='rejected'`

2. **用印申请** (`sealusage`):
   - 位置: 行768-771
   - 行为: 仅记录日志，不更新状态

3. **其他业务对象**:
   - 位置: 行773-777
   - 行为: 
     - 优先设置 `status='rejected'`（如果存在）
     - 否则回退到 `status='pending_approval'`

### 4.2 计划审批的特殊处理（信号处理器）

**文件**: `backend/apps/plan_management/signals.py`

**函数**: `handle_plan_approval_status_change` (行23)

**审批通过后的行为**:

1. **计划启动审批** (`plan_start_approval`):
   - 位置: 行42-47
   - 行为: 调用 `PlanApprovalService.handle_approval_result`
   - 具体逻辑 (在 `plan_approval.py` 行216-229):
     - 计划状态从 `draft` 变为 `published`
     - 调用 `plan.transition_to('published', user=instance.applicant)`

2. **计划取消审批** (`plan_cancel_approval`):
   - 位置: 行59-64
   - 行为: 调用 `PlanApprovalService.handle_approval_result`
   - 具体逻辑 (在 `plan_approval.py` 行233-249):
     - 计划状态从 `in_progress` 变为 `cancelled`
     - 创建 `PlanStatusLog` 记录

**审批驳回后的行为**:

1. **计划启动审批驳回**:
   - 位置: 行48-57
   - 行为: 创建 `PlanStatusLog`，状态不变

2. **计划取消审批驳回**:
   - 位置: 行65-74
   - 行为: 创建 `PlanStatusLog`，状态不变

### 4.3 借款审批的特殊通知

**文件**: `backend/apps/workflow_engine/services.py`

**函数**: `ApprovalEngine._notify_cashier_on_loan_approval` (行787)

**行为**: 审批通过后，通知出纳员角色用户

### 4.4 业务行为代码位置汇总

| 业务对象 | 审批通过行为位置 | 审批驳回行为位置 | 是否在审批引擎中 |
|---------|----------------|----------------|----------------|
| `businessopportunity` | `services.py:718-729` | `services.py:726-727` | 是 |
| `sealborrowing` | `services.py:744-750` | `services.py:766-767` | 是 |
| `sealusage` | `services.py:751-754` (仅日志) | `services.py:768-771` (仅日志) | 是 |
| `loanapplication` | `services.py:756-759` | `services.py:773-777` | 是 |
| `plan` | `signals.py:42-47` + `plan_approval.py:216-229` | `signals.py:48-57` | 否（信号处理器） |
| 其他通用对象 | `services.py:756-759` | `services.py:773-777` | 是 |

---

## 五、实际被使用的模板 vs 废弃模板

### 5.1 有审批实例的模板（已使用）

| 模板代码 | 审批实例数 | 状态 | 结论 |
|---------|-----------|------|------|
| `plan_start_approval` | 73 | active | ✅ 正在使用 |
| `opportunity_approval` | 26 | active | ✅ 正在使用 |
| `seal_usage_approval` | 12 | active | ✅ 正在使用 |
| `loan_approval` | 6 | active | ✅ 正在使用 |
| `plan_cancel_approval` | 3 | active | ✅ 正在使用 |
| `seal_borrowing_approval` | 1 | active | ✅ 正在使用 |

### 5.2 无审批实例的模板（疑似废弃）

| 模板代码 | 审批实例数 | 状态 | 结论 |
|---------|-----------|------|------|
| `administrative_general_approval` | 0 | active | ⚠️ 疑似废弃（通用流程但未使用） |
| `goal_adjustment_approval` | 0 | active | ⚠️ 疑似废弃（无实例） |
| `goal_creation_approval` | 0 | active | ⚠️ 疑似废弃（无实例） |

### 5.3 模板使用情况分析

**正在使用的模板**: 6个
- 计划管理: 2个 (`plan_start_approval`, `plan_cancel_approval`)
- 行政管理: 3个 (`loan_approval`, `seal_borrowing_approval`, `seal_usage_approval`)
- 商机管理: 1个 (`opportunity_approval`)

**疑似废弃的模板**: 3个
- 行政管理: 1个 (`administrative_general_approval`)
- 计划管理: 2个 (`goal_creation_approval`, `goal_adjustment_approval`)

---

## 六、是否存在多套审批系统

### 6.1 结论：**存在两套审批系统并行运行**

### 6.2 证据1：计划管理模块的双重审批机制

**新系统（审批引擎）**:
- 模型: `WorkflowTemplate`, `ApprovalInstance`, `ApprovalRecord`
- 服务: `PlanApprovalService` (在 `plan_approval.py`)
- 流程代码: `plan_start_approval`, `plan_cancel_approval`
- 状态: ✅ 已迁移完成（见 `MIGRATION_TO_APPROVAL_ENGINE.md`）

**旧系统（PlanDecision裁决模型）**:
- 模型: `PlanDecision` (在 `plan_management/models.py`)
- 服务: `plan_decisions.py` 中的 `request_start`, `request_cancel`
- 状态: ⚠️ 保留作为回退机制

**交叉调用证据**:
- 文件: `backend/apps/plan_management/services/plan_decisions.py`
- 行48-50: `request_start` 函数优先使用审批引擎，如果失败则回退到 PlanDecision
- 行103-105: `request_cancel` 函数优先使用审批引擎，如果失败则回退到 PlanDecision

### 6.3 证据2：合同审批的流程查找逻辑

**文件**: `backend/apps/contract_management/views_pages.py`
- 行1368-1382: 先查找 `contract_approval`，如果不存在则回退到 `customer_management_approval`
- 说明: 存在多个流程代码的查找逻辑，可能存在流程配置不一致

### 6.4 证据3：商机审批的流程查找逻辑

**文件**: `backend/apps/opportunity_management/views_opportunity.py`
- 行577-586: 先查找 `opportunity_approval`，如果不存在则通过 `applicable_models` 查找
- 说明: 存在回退机制，但未发现其他审批系统

### 6.5 多套审批系统总结

| 系统名称 | 模型 | 使用范围 | 状态 |
|---------|------|---------|------|
| **审批引擎（新）** | `WorkflowTemplate`, `ApprovalInstance` | 全系统（计划、商机、行政、合同等） | ✅ 主要系统 |
| **PlanDecision（旧）** | `PlanDecision` | 仅计划管理模块 | ⚠️ 回退机制 |

**交叉调用情况**:
- 计划管理模块: 新系统优先，旧系统作为回退
- 其他模块: 仅使用新系统

---

## 七、其他发现

### 7.1 审批服务层架构

**通用服务层**:
- `UniversalApprovalService` (`backend/apps/workflow_engine/services/universal_approval.py`)
- 提供统一的审批接口：`submit_approval`, `approve`, `reject`, `withdraw`

**业务服务层**（继承自 UniversalApprovalService）:
- `LoanApprovalService` (`backend/apps/administrative_management/services/loan_approval.py`)
- `SealBorrowingApprovalService` (`backend/apps/administrative_management/services/seal_borrowing_approval.py`)
- `SealUsageApprovalService` (`backend/apps/administrative_management/services/seal_usage_approval.py`)
- `PlanStartApprovalService` (`backend/apps/plan_management/services/plan_approval_v2.py`)
- `PlanCancelApprovalService` (`backend/apps/plan_management/services/plan_approval_v2.py`)

### 7.2 审批后业务逻辑的集中度

**高度集中**: 大部分业务逻辑在 `ApprovalEngine._update_business_object_status` 中
**例外**: 计划审批通过信号处理器 (`plan_management/signals.py`)

### 7.3 模板配置问题

1. **适用模型配置不一致**:
   - `seal_borrowing_approval`: `['{sealborrowing}', 'sealborrowing']` (存在重复和格式不一致)
   - `seal_usage_approval`: `['{sealusage}']` (格式不一致，应使用 `sealusage`)

2. **通用流程未使用**:
   - `administrative_general_approval`: 适用模型为空，无审批实例

---

## 八、取证结论

### 8.1 模板总数
- **10个审批模板**，其中 **6个正在使用**，**3个疑似废弃**

### 8.2 业务对象覆盖
- **5种业务对象类型**正在使用审批流程：`plan`, `businessopportunity`, `sealusage`, `loanapplication`, `sealborrowing`

### 8.3 审批触发方式
- **7个触发入口**，包括手动提交、自动触发、服务层调用

### 8.4 业务逻辑集中度
- **高度集中**：大部分业务逻辑在审批引擎的 `_update_business_object_status` 方法中
- **例外**：计划审批使用信号处理器

### 8.5 多套系统
- **存在两套系统**：审批引擎（新）+ PlanDecision（旧，仅计划管理，作为回退）

### 8.6 潜在问题
1. 3个模板疑似废弃但状态为 `active`
2. 适用模型配置格式不一致
3. 计划管理模块存在新旧系统并行
4. 业务逻辑部分在审批引擎中，部分在信号处理器中

---

**取证完成时间**: 2026-02-05  
**取证人员**: Cursor AI  
**取证原则**: 只读、不修改、基于事实
